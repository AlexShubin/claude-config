# claude-config

Global Claude Code configuration, synced across machines.

## Setup on a new machine

```sh
git clone git@github.com:AlexShubin/claude-config.git ~/claude-config
ln -s ~/claude-config/CLAUDE.md ~/.claude/CLAUDE.md
```

Sync: `git pull` / `git push`.
